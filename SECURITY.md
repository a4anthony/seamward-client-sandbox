# Security

This repository contains synthetic test data only.

Do not commit Seamward ingest tokens, GitHub credentials, provider secrets, webhook secrets, private keys, or real candidate data. Keep local values in `.env`, which is ignored by Git.

Report a suspected credential exposure by opening a private security advisory on the repository rather than a public issue.
